#
# This file contain vars and locals that we want to override in parent terragrunt.hcl.
#

tg_remote_state:
  # Env-specific state file location in S3
  s3_region: "us-east-1"
  s3_bucket: "fsiem-terraform-ftn"
  # Env-specific state file lock table in DynamoDb
  dynamodb_table: "tf_state_lock_dev"
