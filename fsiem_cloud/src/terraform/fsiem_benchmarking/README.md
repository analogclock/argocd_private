Resources required for benchmarking a fortisiem cloud stack are setup
in the fsiem_benchmarking folder

# Fsiem benchmarking build configuration
All env variables are optional
export CI_ENVIRONMENT_NAME=playground # dev/playground
